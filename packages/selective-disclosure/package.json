{
  "name": "@stellar-privacy/selective-disclosure",
  "version": "0.1.0",
  "description": "Regulatory viewing keys and selective disclosure proofs for the Stellar Privacy Layer.",
  "license": "MIT OR Apache-2.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "lint": "biome check src/",
    "typecheck": "tsc --noEmit",
    "format": "biome format --write src/",
    "prepublishOnly": "npm run build && npm test"
  },
  "dependencies": {
    "@stellar-privacy/sdk": "^0.1.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.7",
    "@types/node": "^26.2.0",
    "@vitest/coverage-v8": "^4.1.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.0"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
