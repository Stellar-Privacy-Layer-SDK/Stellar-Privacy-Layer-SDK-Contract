## Summary

<!-- What does this PR do and why? Keep the core project idea unchanged. -->

## Changes

- [ ] Feature / fix:
- [ ] Packages touched: sdk / selective-disclosure / dapp / contract / prover / infra

## Testing

- [ ] `make check` passes (cargo fmt, clippy -D warnings, npm lint/format/typecheck/coverage/build)
- [ ] `make test` passes (cargo + npm tests)
- [ ] `npm audit` and `cargo audit` report no new vulnerabilities
- [ ] New behavior has tests; coverage gates (≥80%) still pass

## Security & docs

- [ ] No secrets committed (`.env`, `DEPLOYER_SECRET` stay out of git)
- [ ] README / SECURITY.md updated if behavior, env vars, or security posture changed
- [ ] `deny.toml` exceptions updated if the dependency graph changed

## Notes for reviewers

<!-- Anything unusual, migration steps, follow-up work. -->
